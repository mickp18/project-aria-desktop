# project-aria-desktop
Desktop app for project aria thesis

## Requirements
It needs Python3.11 (Python 3.12 not working)

### Virtual env
create virtual env, then install requirements 
```pip install -r requirements.txt```